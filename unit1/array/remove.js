const numbers = [100, 200, 300, 400, 500]

//Pop removes elements from the END
numbers.pop()
console.log(numbers);

//Shift removes element from the beginning of an array 
//Also you can simply store the poped value inside an variable
const firstNumbers = numbers.shift()
console.log(numbers);
console.log(firstNumbers);
